#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

echo "[pre-push] running strict clippy"
cargo clippy --all-targets --all-features -- -D warnings

echo "[pre-push] running tests"
cargo test --tests -- --test-threads=1
